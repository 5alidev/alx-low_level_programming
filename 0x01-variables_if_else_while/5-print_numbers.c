#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int firstNumber = 0;

	for (firstNumber = 0; firstNumber < 10; firstNumber++)
	{
		printf("%d", firstNumber);
	}
	putchar('\n');
	return (0);
}
