#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 *
 * @s: the string to return its length
 *
 * Return: length of a string
 */
int _strlen(char *s)
{
	int strLen;

	strLen = strlen(s);
	return (strLen);
}
